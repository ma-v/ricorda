<ul>
	<% @memories.each do |memory| %>
		<li class=<%= "#{memory.cultural_good.cultural_type.downcase}-card" %>>
			<h2><%= memory.cultural_good.title %></h2> 
			<h3><%= memory.cultural_good.creator.name %></h3> 
			<p>Location : <%= memory.venue.name %></p> 
			<p>Rating : 
				<% memory.rating.times do %>
					<i class="fas fa-star"></i>
				<% end %>
			</p>
			<p>"<%= memory.review %>"</p> 
		</li>
	<% end %>
</ul>

<%= render 'form', memory: @memory %>